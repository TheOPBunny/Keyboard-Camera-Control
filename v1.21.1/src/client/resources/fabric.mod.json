{
  "schemaVersion": 1,
  "id": "camera-control",
  "version": "${version}",
  "name": "Camera Control",
  "description": "Move your camera with remappable keys. Adjust speed on the fly.",
  "authors": ["Hassan"],
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "client": [
      "com.hassanmod.cameracontrol.CameraControlClient"
    ]
  },
  "mixins": [
    {
      "config": "camera-control.mixins.json",
      "environment": "client"
    }
  ],
  "depends": {
    "fabricloader": ">=0.15.0",
    "minecraft": "~1.21.1",
    "java": ">=21"
  }
}
